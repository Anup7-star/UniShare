import 'package:unishare/shared/models/models.dart';

final List<DeliveryRequest> mockDeliveries = [
  DeliveryRequest(
    id: 'd1',
    requesterId: 'u2',
    pickupLocationId: 'loc_masala',
    destinationId: 'loc_kumaon',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Food delivery from Masala Mix',
    reward: 25.0,
    priceCeiling: 25.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.medium,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(minutes: 15)),
  ),
  DeliveryRequest(
    id: 'd2',
    requesterId: 'u4',
    pickupLocationId: 'loc_cs_dept',
    destinationId: 'loc_library',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Document to be dropped at Central Library',
    reward: 15.0,
    priceCeiling: 15.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.low,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(hours: 1)),
  ),
  DeliveryRequest(
    id: 'd3',
    requesterId: 'u5',
    pickupLocationId: 'loc_gate',
    destinationId: 'loc_nilgiri',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Electronics parcel (charger)',
    reward: 40.0,
    priceCeiling: 40.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.high,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(minutes: 5)),
  ),
  DeliveryRequest(
    id: 'd4',
    requesterId: 'u6',
    pickupLocationId: 'loc_lhc',
    destinationId: 'loc_aravali',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Books (CLRS and OS)',
    reward: 20.0,
    priceCeiling: 20.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.medium,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(minutes: 30)),
  ),
  DeliveryRequest(
    id: 'd5',
    requesterId: 'u7',
    pickupLocationId: 'loc_shopping',
    destinationId: 'loc_jwalamukhi',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Fragile package (Glassware)',
    reward: 50.0,
    priceCeiling: 50.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.urgent,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(minutes: 10)),
  ),
  DeliveryRequest(
    id: 'd6',
    requesterId: 'u8',
    pickupLocationId: 'loc_amul',
    destinationId: 'loc_blk_vi',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Food from Amul Parlour',
    reward: 20.0,
    priceCeiling: 20.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.low,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(hours: 2)),
  ),
];


