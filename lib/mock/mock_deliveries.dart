import 'package:unishare/shared/models/models.dart';

final List<DeliveryRequest> mockDeliveries = [
  DeliveryRequest(
    id: 'd1',
    requesterId: 'u2',
    pickupLocationId: 'loc_gate',
    destinationId: 'loc_a_block',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Parcel from Main Gate to A Block',
    reward: 25.0,
    priceCeiling: 25.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.medium,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(minutes: 15)),
  ),
  DeliveryRequest(
    id: 'd2',
    requesterId: 'u4',
    pickupLocationId: 'loc_gate',
    destinationId: 'loc_ab2',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Document delivery to AB2',
    reward: 15.0,
    priceCeiling: 15.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.low,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(hours: 1)),
  ),
  DeliveryRequest(
    id: 'd3',
    requesterId: 'u5',
    pickupLocationId: 'loc_gate',
    destinationId: 'loc_c_block',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Electronics parcel (charger) to C Block',
    reward: 40.0,
    priceCeiling: 40.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.high,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(minutes: 5)),
  ),
  DeliveryRequest(
    id: 'd4',
    requesterId: 'u6',
    pickupLocationId: 'loc_gate',
    destinationId: 'loc_north_square',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Books to North Square',
    reward: 20.0,
    priceCeiling: 20.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.medium,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(minutes: 30)),
  ),
  DeliveryRequest(
    id: 'd5',
    requesterId: 'u7',
    pickupLocationId: 'loc_gate',
    destinationId: 'loc_gazebo',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Fragile package to Gazebo',
    reward: 50.0,
    priceCeiling: 50.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.urgent,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(minutes: 10)),
  ),
  DeliveryRequest(
    id: 'd6',
    requesterId: 'u8',
    pickupLocationId: 'loc_gate',
    destinationId: 'loc_f_block',
    packageType: PackageType.parcel,
    packageSize: PackageSize.medium,
    note: 'Package to F Block',
    reward: 20.0,
    priceCeiling: 20.0,
    preferredTime: DateTime.now().add(const Duration(hours: 1)),
    urgency: UrgencyLevel.low,
    status: DeliveryStatus.open,
    createdAt: DateTime.now().subtract(const Duration(hours: 2)),
  ),
];


